<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/grid_item_container"
    android:layout_width="match_parent"
    android:layout_height="185dp">

    <!--suppress AndroidLintContentDescription -->
    <ImageView
        android:id="@+id/thumbnail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="fitXY" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="@dimen/grid_item_rel_layout_height"
        android:layout_gravity="bottom"
        android:background="@color/translucent"
        android:padding="@dimen/grid_item_rel_layout_padding">


        <TextView
            android:id="@+id/garden_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:alpha="0.87"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/white"
            android:textSize="@dimen/tablet_text_size" />
    </RelativeLayout>

</FrameLayout>